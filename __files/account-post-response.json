{
  "data": [
    {
      "id": "{{#assign 'id'}}{{randomValue type='UUID'}}{{/assign}}{{id}}",
      "name": "{{jsonPath request.body '$.name'}}",
      "lastName": "{{jsonPath request.body '$.lastName'}}"
    }
  ]
}
